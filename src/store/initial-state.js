import sortConstants from "./sort-constants"

const initialState = {
    stops: [0, 1, 2, 3],
    sort: sortConstants.price,
    tickets: [],
    isLoading: true
}

export default initialState